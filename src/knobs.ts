import { atom } from "nanostores";

export const $minimumPeak = atom(2.5);
export const $activationThreshold = atom(0.25);
export const $deactivationThreshold = atom(0.2);
export const $maxLength = atom(10);
export const $decayEasing = atom(1.25);
